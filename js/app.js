function weather(weather) {
    let result;
    if (weather === 'winter'){
        result = "Too much snow";
    } else {
        result = "Too much snow";
    }
    return result;
}
console.log (weather("winter"));